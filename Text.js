import React, { useState } from 'react';
import { Text, Stylesheet } from 'react-native';

const Texto = () => {
    const[titleText, setTitleText] = useState("Hey muy buenas a todos guapísimos");
    const bodyText = "Aquí en un nuevo video para el canal.";
};